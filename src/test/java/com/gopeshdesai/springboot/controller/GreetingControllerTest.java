package com.gopeshdesai.springboot.controller;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.web.client.TestRestTemplate;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.test.context.web.WebAppConfiguration;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Created by gopeshdesai on 10/9/16.
 */

//@RunWith(SpringJUnit4ClassRunner.class)
//@SpringBootApplication(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
//@WebAppConfiguration

public class GreetingControllerTest {

    //@Autowired
    //private TestRestTemplate restTemplate;

    //@Before
    //public void setUp() {
    //}

    //@Test
    //public void getMessage() {
    //    String body = this.restTemplate.getForObject("/greeting",String.class, "Hello");
      //  assertThat(body).isEqualTo("hello");
    //}

}
